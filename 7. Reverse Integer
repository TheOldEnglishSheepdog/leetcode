class Solution:
    def reverse(self, x: int) -> int:
        s=str(x)
        if s[-1]=='0':
            integer2=int(x/10)
        else:
            integer2=x
        s1=str(integer2)  
        if integer2 < 0:
            return s1[0]+s1[-1:0:-1]
        else: 
            return s1[-1::-1]
